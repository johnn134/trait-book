import { CSSPropertiesMap } from "@/globals";

const style: CSSPropertiesMap = {
  typography: {
    fontFamily: "monospace",
    fontWeight: 700,
    letterSpacing: ".3rem",
    color: "inherit",
    textDecoration: "none",
  },
};

export default style;
